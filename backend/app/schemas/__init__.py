"""Pydantic schemas exposed via the API.

Submodules define input and output models for different resources such
as authentication, cameras, ROI definitions and events. Schemas are
used to validate request bodies and to serialise responses.
"""